apiVersion: v1
kind: Secret
metadata:
  name: wa-bridge-secrets
  namespace: nexus
type: Opaque
stringData:
  # Phone numbers without + prefix (e.g., 447123456789)
  bot-phone-1: "PHONE_NUMBER_1"
  # bot-phone-2: "PHONE_NUMBER_2"
