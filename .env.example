# NEXUS configuration
TELEGRAM_BOT_TOKEN=your-bot-token-here
AUTHORIZED_USER_ID=your-telegram-user-id

# Optional overrides
# CLAUDE_BIN=/usr/bin/claude
# CLAUDE_CWD=/home/puretensorai
CLAUDE_TIMEOUT=1800

# Engine backend: claude_code (default), anthropic_api, ollama, openai_compat, gemini_cli, codex_cli
# ENGINE_BACKEND=claude_code

# Anthropic API backend (requires: pip install anthropic)
# ANTHROPIC_API_KEY=sk-ant-...

# Ollama backend (local models)
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=qwen3:30b-a3b
# OLLAMA_TOOLS_ENABLED=true       # Enable tool calling (bash, read, write, edit, glob, grep)
# OLLAMA_TOOL_MAX_ITER=25         # Max tool-call loop iterations per request
# OLLAMA_TOOL_TIMEOUT=30          # Timeout per bash command (seconds)

# OpenAI-compatible backend (Grok, Mistral, vLLM, etc.)
# OPENAI_COMPAT_URL=http://localhost:8080
# OPENAI_COMPAT_KEY=
# OPENAI_COMPAT_MODEL=gpt-4o

# Gemini CLI backend (stub)
# GEMINI_BIN=/usr/bin/gemini

# Codex CLI backend (stub)
# CODEX_BIN=/usr/bin/codex

# Dispatcher — fast API responses (no Claude needed)
WEATHER_DEFAULT_LOCATION=Windsor,UK
PROMETHEUS_URL=http://MON2_TAILSCALE_IP:9090
DARWIN_API_TOKEN=
GOLD_API_KEY=

# Daily Intelligence Snippet — email config
SNIPPET_SMTP_HOST=mail.privateemail.com
SNIPPET_SMTP_PORT=587
SNIPPET_SMTP_USER=
SNIPPET_SMTP_PASS=
SNIPPET_FROM=
SNIPPET_TO=
