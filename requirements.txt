python-telegram-bot>=21.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
Pillow>=10.0.0
edge-tts>=6.1.0

# Optional backend dependencies (install only what you need):
# anthropic>=0.40.0        # for ENGINE_BACKEND=anthropic_api
# aiohttp>=3.9.0           # already installed — used by ollama/openai_compat streaming
